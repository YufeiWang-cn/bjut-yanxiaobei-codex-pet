# 更新记录

## 0.2.6 — 2026-09-22

- 修复部分全新电脑始终显示“空闲中”：任务探测优先读取 Codex App Server 的运行时 `status` 与 `thread/status/changed`，本地会话和桌面日志改为兼容回退。
- 固定 `thread/list` 只查询交互式 `cli` / `vscode` 任务；兼容不返回 `source`、使用 `thread_source`、自定义 `CODEX_HOME` 和 App Server 返回真实数据目录的客户端，同时继续排除子任务、内部与临时会话。
- 增加 Microsoft Store / MSIX 应用包日志目录发现，并避免在隔离用户环境中误扫另一个系统账号的会话目录。
- Windows 新增可双击 `Update.vbs`：验证固定 GitHub 下载地址、发布版本和 SHA256 后，关闭旧进程并在原目录替换核心文件；设置和缓存保留。
- Windows 新增可双击 `Uninstall.vbs`：可选择保留习惯与缓存，或连同本地数据全部清除；两种模式都会移除核心文件和跟随启动项。
- Windows 更新弹窗改为“立即更新”；发布包根目录直接提供更新与卸载入口，卸载不放入桌宠右键菜单，避免误触。
- 启动自动检查失败时静默快速重试一次；“暂时忽略”只影响本次运行，“忽略此版本”会一直生效到出现更高版本。
- Windows 和 Mac 在 Codex 完全退出后约 1 秒自动关闭桌宠；该行为不受“跟随 Codex 启动 / 显示”开关影响。
- 精简右键菜单文字并统一操作顺序：刷新额度与状态、清除完成 / 错误提醒、使用说明、检查更新。
- Mac 发布包根目录新增双击卸载工具，可选择保留或清除设置，并将核心目录移入废纸篓；卸载入口同样不放入桌宠菜单。
- 增加完全隔离的新用户目录任务探测、App Server 状态、更新包校验、保留/清除卸载和 Codex 退出生命周期测试。

## 0.2.5 — 2026-09-22

- 修复部分电脑下载后任务始终显示空闲：不再把新的顶层会话来源名称误判为未知任务；Codex 桌面真实轮次可先建立受控的顶层任务身份，后续子任务元数据仍可撤销误识别。
- 增加最近会话主动发现，即使桌面日志目录缺失或客户端暂未返回任务列表，也能从本机最近会话生命周期识别正在执行的顶层 Codex 任务；历史已完成任务在启动时保持静默。
- Windows 兼容更多 Codex 日志安装目录，并在标准 t0 日志不存在时回退到当前桌面日志。
- 取消普通 ChatGPT 聊天状态读取，任务活动只跟踪 Codex：当前客户端没有面向第三方的稳定 Chat 生命周期接口；窗口控件只能确认当前页面，后台日志也不保证提供完整的开始与结束事件，继续使用会造成切页消失或长期误报。
- 移除 Windows 窗口探针、Mac 辅助功能授权和桌面日志中的普通 Chat 状态转换；不再读取 Chat 控件名称、窗口标题或普通聊天生命周期。兼容 `ChatGPT.exe` 进程名与相关日志目录仅用于识别部分新版 Codex 客户端，不代表跟踪普通 Chat。
- 任务活动保留 `kind: codex` 与界面 `Codex` 来源标识；增加普通 Chat 日志不得改变状态的回归测试，并同步 Windows、macOS、文档和发布包。

## 0.2.4 — 2026-09-20

- 新安装首次启动默认跟随 Codex：Windows 自动设置当前用户启动观察器，Mac 打包版尝试设置登录项并默认跟随显示；保留已有用户的关闭选择，右键可随时关闭或重新开启。
- Windows、Mac 增强版右键增加离线“使用说明”；修复 Mac 关闭跟随后桌宠仍隐藏的问题。
- 更新提醒优先提取 Release 中的新增、修复、优化条目；Windows 更新窗口支持长文字自动换行和滚动，Mac 保持简短说明及完整发布页入口。
- 修正自动审查误报“需要确认”：按每轮实际审批人区分 `auto_review` 和人工审批；自动审查的权限请求继续显示进行中，明确展示给用户的审批界面及需要回复的问题才进入等待，并在响应后恢复。
- Windows 和 Mac 增强版同步加快额度刷新：直接应用 Codex 额度通知，任务执行期间提高补查频率，任务结束后补查；手动刷新不再因已有请求而丢失，界面区分真实额度更新时间与活动心跳。
- Windows 更新检查减少额外 PowerShell 进程启动：常见的固定系统代理与直连改用轻量请求；PAC/自动代理及请求异常仍保留原网络路径作兼容，查询总时限不变。
- 修复共享代理出口触发 GitHub 匿名 REST API 60 次/小时限流后检查更新失败：优先读取 GitHub 官方 Releases Atom 订阅，订阅不可用才回退 API；无需令牌。
- Windows 与 Mac 都保留系统代理/直连支持，查询继续显示安全的发布页链接和简短更新说明；增加 Atom、限流回退和跨平台回归测试。
- 更新查询在 Windows 跟随系统代理/PAC，也支持 `HTTPS_PROXY`；Mac 使用 Electron 系统网络代理。未启用代理时仍可直连；代理不可用时按时限提示，不影响桌宠启动。
- 增加代理请求、中文发布内容及错误/超时的本地回归测试；检查更新仍不自动下载或安装。
- 更新查询增加约 5 秒的全程截止时间；手动点击立即显示检查进度，自动检查期间的手动请求也会收到结果。
- GitHub 暂时不可达或限制请求时，可直接打开发布页手动查看；Mac 查询约 5 秒、Windows 含代理检测和子进程约 7.5 秒截止。
- 修复 Windows PowerShell 5.1 对中文更新说明的编码解析失败；更新结果使用 ASCII 安全 JSON，并明确按 UTF-8 读取。
- 当本地版本高于 GitHub 已发布版本时，提示“没有更高的新版本”，不再误称本地版本已发布。
- Windows、Mac 增强版新增启动检查更新和右键主动检查；可打开发布页、忽略当前版本或暂时忽略到下次启动。
- 增加原生素材与 Windows 卸载脚本；Mac 提供菜单卸载和辅助脚本，清理本项目设置与启动项，不触碰 Codex 账号与其他宠物。
- 审批请求关联到对应任务，补全不带任务 ID 的解除事件；短暂自动审批不再闪现等待状态。
- 同步测试、文档及四份发布压缩包。

## 0.2.3 — 2026-09-06

- 适配新版 Codex 的审批事件格式：从结构化会话记录识别明确的 `request_permissions` 和用户输入请求，审批出现时恢复“等待批准”状态。
- 由 Codex 自动审查器处理的提升权限命令保持“进行中”；只有桌面确实显示审批窗口或会话明确等待用户输入时才切换“等待批准”，避免两个状态来回闪烁。
- 审批响应缺少 `conversationId` 时，以请求 ID 关联所属主任务；批准或拒绝只解除对应请求，不会误清除其他任务的等待状态。
- 会话中的无关工具输出不再解除等待；匹配的审批结果会继续任务，任务结束后恢复空闲状态。
- Windows 与 macOS 共享运行时代码、资源清单和回归测试同步更新，并重新生成四个发布包及 SHA256 校验文件。

## 0.2.2 — 2026-09-03

- 任务队列只展示来源已确认的主任务；标题生成、后台建议等内部临时会话和所有 subagent（包括 guardian、review、compact）不再单独计数或触发桌宠动作。
- Windows 与 Mac 共用会话来源过滤；会话标题不能单独作为有效任务的依据。未知来源在确认前不展示。
- 打开或恢复历史任务只校正状态，不产生新的完成/出错提醒；首次读取已完成的会话历史静默建立基线。
- 真实主任务的持续思考、审批、结束提示和多任务独立状态保留；增加任务来源及历史恢复回归测试。
- 活动快照增加 bridgeVersion，便于确认实际运行版本，而不只核对磁盘文件。
- 同步现有文档、Mac 运行副本和四个下载包；保留维护者对 FAQ、展示说明及目录的删改，移除失效引用和旧副本，不恢复已删除文档。
- 增加逐文件源码审计及发布包一致性检查；发布物排除依赖、测试产物、备份和本地状态数据，完整源码包保留测试脚本。

## 0.2.1 — 2026-09-03

- 新增从下载、解压到启动/排错的零基础说明，以及可直接双击离线阅读的 START-HERE.html。
- 四个发布包都带 VERSION.txt、离线指南和完整使用说明；加强版本号、规范资源及发布归档的一致性校验。
- 区分最新发布、旧私人版、运行中进程；磁盘更新不再被描述成运行中桌宠已经更新。
- 明确 Mac 仍为实验性源码构建流程，真实一键安装、签名/公证和实机验收尚未完成。
- 本次不更改角色像素、动画节奏或任务状态行为。

## 0.2.0 — 2026-09-03

- 统一为个人制作、个人维护，区分原生、Windows、macOS 三种版本。
- 新增 macOS Electron 增强版：透明桌宠、额度/重置次数、折叠任务、任务链接、菜单栏、拖动边界、显示开关、路径选择及主动启用的登录/通知入口。
- Mac 复用 0.1.2 生命周期修复与 3.6 秒出错动作；增加平台路径和无兼容桌面日志时的近期根会话降级。
- 新增 Apple Silicon / Intel 构建入口、依赖锁文件、手动 Mac 构建工作流与源码启动器。
- 新增 Mac 单元测试和实际 Electron 演示渲染检查；Windows 回归通过。Mac 实机、签名、公证、登录项及真实连接仍待验收。
- 全面更新 README、安装、FAQ、隐私、架构、展示和发布文档；发布物新增 Mac 源码包，不冒充现成签名安装器。

## 0.1.2 — 2026-09-03

- 修复任务实际完成却仍显示“思考中”：从本机会话增量读取逐轮生命周期，不再依赖系统完成通知。
- 处理主动中止、迟到的旧轮事件、重复通知、日志轮转与重启恢复；已清除提醒不会因回读再次出现。
- 思考进展不再擅自解除等待审批；带 ID 的审批逐个跟踪。
- 修复错误提醒未按约定在 10 分钟后过期。
- 增加活动心跳；数据桥退出或活动快照过期时显示离线与未知计数，恢复连接后重建当前状态。
- 新增 25 项生命周期回归与界面断线恢复检查；真实历史回放验证完成后运行计数归零。
- 当时的 Mac 边界：原生素材需兼容 v2 宿主并待实机验证，0.1.2 增强版仅支持 Windows；后续 0.2.0 新增 Mac 源码版。

## 0.1.1 — 2026-09-03

- 出错动作改为轻微垂头与眼睑变化，移除大幅鞠躬式起伏。
- Windows 增强版的出错循环由 1.9 秒延长到 3.6 秒，并增加停顿；其他 8 组动作节奏保持不变。
- 统一 `animation-timing.json`，实际播放与展示 GIF 共用配置；原生宿主固定时序的预览单独存放。
- 新增动画节奏、同状态不重置、状态退出和帧数匹配回归测试。
- 同步原生图集、独立版逐帧文件、展示图与发布包。

## 0.1.0 — 2026-09-03

首次规范化发布目录，基于此前的本机版本整理。

- 分离 Codex 原生素材版与 Windows 独立增强版，统一命名为 `bjut-yanxiaobei-codex-pet`。
- 保留原生 v2 图集与现有动作素材，不改变角色。
- 独立版 PNG 与最终图集统一，替换早期中间帧的绿边；未重新生成角色图像。
- 保留额度、重置次数、任务活动、状态动画、屏幕边界限制、右键菜单与恢复原 Codex 窗口功能。
- 新增无硬编码本机运行时路径的启动入口；运行数据转移到用户本地应用数据目录。
- 跟随 Codex 启动改为默认关闭、用户主动开启，提供启用与卸载脚本。
- 未返回重置次数时显示未知，而非错误地视为零。
- 增加安装、FAQ、隐私、架构、发布说明与虚构数据效果图。
- 增加隔离安装与自启动测试，保留布局、状态、窗口恢复、任务跳转等回归测试。

代码与不含角色图像的项目文档采用 MIT，角色素材不适用 MIT，使用限制见 FAQ。当前未提供签名 EXE、已签名/公证 Mac 安装包、自动更新、Linux 增强版或稳定的官方插件集成。
