# 更新记录

## 0.2.3 — 2026-09-06

- 适配新版 Codex 的审批事件格式：从结构化会话记录识别明确的 `request_permissions` 和用户输入请求，审批出现时恢复“等待批准”状态。
- 由 Codex 自动审查器处理的提升权限命令保持“进行中”；只有桌面确实显示审批窗口或会话明确等待用户输入时才切换“等待批准”，避免两个状态来回闪烁。
- 审批响应缺少 `conversationId` 时，以请求 ID 关联所属主任务；批准或拒绝只解除对应请求，不会误清除其他任务的等待状态。
- 会话中的无关工具输出不再解除等待；匹配的审批结果会继续任务，任务结束后恢复空闲状态。
- Windows 与 macOS 共享运行时代码、资源清单和回归测试同步更新，并重新生成四个发布包及 SHA256 校验文件。

## 0.2.2 — 2026-09-03

- 任务队列只展示来源已确认的主任务；标题生成、后台建议等内部临时会话和所有 subagent（包括 guardian、review、compact）不再单独计数或触发桌宠动作。
- Windows 与 Mac 共用会话来源过滤；会话标题不能单独作为有效任务的依据。未知来源在确认前不展示。
- 打开或恢复历史任务只校正状态，不产生新的完成/出错提醒；首次读取已完成的会话历史静默建立基线。
- 真实主任务的持续思考、审批、结束提示和多任务独立状态保留；增加任务来源及历史恢复回归测试。
- 活动快照增加 bridgeVersion，便于确认实际运行版本，而不只核对磁盘文件。
- 同步现有文档、Mac 运行副本和四个下载包；保留维护者对 FAQ、展示说明及目录的删改，移除失效引用和旧副本，不恢复已删除文档。
- 增加逐文件源码审计及发布包一致性检查；发布物排除依赖、测试产物、备份和本地状态数据，完整源码包保留测试脚本。

## 0.2.1 — 2026-09-03

- 新增从下载、解压到启动/排错的零基础说明，以及可直接双击离线阅读的 START-HERE.html。
- 四个发布包都带 VERSION.txt、离线指南和完整使用说明；加强版本号、规范资源及发布归档的一致性校验。
- 区分最新发布、旧私人版、运行中进程；磁盘更新不再被描述成运行中桌宠已经更新。
- 明确 Mac 仍为实验性源码构建流程，真实一键安装、签名/公证和实机验收尚未完成。
- 本次不更改角色像素、动画节奏或任务状态行为。

## 0.2.0 — 2026-09-03

- 统一为个人制作、个人维护，区分原生、Windows、macOS 三种版本。
- 新增 macOS Electron 增强版：透明桌宠、额度/重置次数、折叠任务、任务链接、菜单栏、拖动边界、显示开关、路径选择及主动启用的登录/通知入口。
- Mac 复用 0.1.2 生命周期修复与 3.6 秒出错动作；增加平台路径和无兼容桌面日志时的近期根会话降级。
- 新增 Apple Silicon / Intel 构建入口、依赖锁文件、手动 Mac 构建工作流与源码启动器。
- 新增 Mac 单元测试和实际 Electron 演示渲染检查；Windows 回归通过。Mac 实机、签名、公证、登录项及真实连接仍待验收。
- 全面更新 README、安装、FAQ、隐私、架构、展示和发布文档；发布物新增 Mac 源码包，不冒充现成签名安装器。

## 0.1.2 — 2026-09-03

- 修复任务实际完成却仍显示“思考中”：从本机会话增量读取逐轮生命周期，不再依赖系统完成通知。
- 处理主动中止、迟到的旧轮事件、重复通知、日志轮转与重启恢复；已清除提醒不会因回读再次出现。
- 思考进展不再擅自解除等待审批；带 ID 的审批逐个跟踪。
- 修复错误提醒未按约定在 10 分钟后过期。
- 增加活动心跳；数据桥退出或活动快照过期时显示离线与未知计数，恢复连接后重建当前状态。
- 新增 25 项生命周期回归与界面断线恢复检查；真实历史回放验证完成后运行计数归零。
- 当时的 Mac 边界：原生素材需兼容 v2 宿主并待实机验证，0.1.2 增强版仅支持 Windows；后续 0.2.0 新增 Mac 源码版。

## 0.1.1 — 2026-09-03

- 出错动作改为轻微垂头与眼睑变化，移除大幅鞠躬式起伏。
- Windows 增强版的出错循环由 1.9 秒延长到 3.6 秒，并增加停顿；其他 8 组动作节奏保持不变。
- 统一 `animation-timing.json`，实际播放与展示 GIF 共用配置；原生宿主固定时序的预览单独存放。
- 新增动画节奏、同状态不重置、状态退出和帧数匹配回归测试。
- 同步原生图集、独立版逐帧文件、展示图与发布包。

## 0.1.0 — 2026-09-03

首次规范化发布目录，基于此前的本机版本整理。

- 分离 Codex 原生素材版与 Windows 独立增强版，统一命名为 `bjut-yanxiaobei-codex-pet`。
- 保留原生 v2 图集与现有动作素材，不改变角色。
- 独立版 PNG 与最终图集统一，替换早期中间帧的绿边；未重新生成角色图像。
- 保留额度、重置次数、任务活动、状态动画、屏幕边界限制、右键菜单与恢复原 Codex 窗口功能。
- 新增无硬编码本机运行时路径的启动入口；运行数据转移到用户本地应用数据目录。
- 跟随 Codex 启动改为默认关闭、用户主动开启，提供启用与卸载脚本。
- 未返回重置次数时显示未知，而非错误地视为零。
- 增加安装、FAQ、隐私、架构、发布说明与虚构数据效果图。
- 增加隔离安装与自启动测试，保留布局、状态、窗口恢复、任务跳转等回归测试。

代码与不含角色图像的项目文档采用 MIT，角色素材不适用 MIT，使用限制见 FAQ。当前未提供签名 EXE、已签名/公证 Mac 安装包、自动更新、Linux 增强版或稳定的官方插件集成。
